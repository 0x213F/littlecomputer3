/*
 * `mem.h` declares the LC-3 memory map
 */

#include "uint.h"

extern uint *trp_vect_tbl;
extern uint trp_vect_tbl_limit;

extern uint *int_vect_tbl;
extern uint int_vect_tbl_limit;

extern uint *sup_stack;
extern uint sup_stack_limit;

extern uint *usr_stack;
extern uint sup_stack_limit;

extern uint *io_dev;
extern uint io_dev_limit;

